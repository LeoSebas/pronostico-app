import { createSlice, configureStore } from "@reduxjs/toolkit";

// Slice for AppState 

const appState = createSlice({
    name: "APP_STATE",
    initialState,
    reducers: {
            
    }

})

